# QuizMicroservice
Uses Eurekaclient and OpenFeign to communicate between the micro services. Uses spring api gateway for a common api gateway to communicate between the servers. 
Load Balancing between multiple instances of servers is another feature.
